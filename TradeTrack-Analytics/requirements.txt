# TradeTrack Analytics — Python dependencies
# Install:  pip install -r requirements.txt
# Tested on Python 3.9-3.13.

# --- core analysis ---------------------------------------------------------
pandas>=2.0
numpy>=1.24

# --- visualisation --------------------------------------------------------
matplotlib>=3.7
plotly>=5.18            # interactive HTML chart exports
seaborn>=0.13           # used for a small number of distribution plots

# --- machine learning -----------------------------------------------------
scikit-learn>=1.3
xgboost>=2.0            # optional: falls back to sklearn GradientBoosting if
                        # the native library cannot load (needs OpenMP —
                        # `brew install libomp` on macOS)

# --- notebook -------------------------------------------------------------
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.0

# --- reporting ------------------------------------------------------------
tabulate>=0.9           # DataFrame.to_markdown() for the generated reports
